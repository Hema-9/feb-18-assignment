code:
def sum_primes(n):
    sum = 0
    
    for num in range(n):
        if num <= 1:
            continue
        for i in range(2, num):
            if (num % i) == 0:
                break
        else:
            sum+= n

    return sum

print(sum_primes(10))
print(sum_primes(100))
out put:
40
2500
